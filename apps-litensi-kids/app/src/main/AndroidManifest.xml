<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- (G3.9) Permission untuk GPS tracking background (SDK 29+ Android 10+) → user harus pilih "Allow all the time" -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!-- Permission agar GPS worker restart otomatis setelah HP di-reboot (nanti BOOT_COMPLETED Receiver) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Permission untuk cek status koneksi sebelum upload GPS (optional monitoring) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <!-- F5.2: Firebase Cloud Messaging Service — menerima push notifikasi & refresh token -->
        <!-- Pakai FQN (Full Qualified Name) com.example.* karena manifest namespace = com.parental.litensikids
             tapi actual source code berada di package com.example (sesuai MainActivity L25). -->
        <service
            android:name="com.example.service.LitensiFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <!-- (G3.9) BroadcastReceiver untuk menangkap Geofence ENTER/EXIT event dari Play Services GeofencingClient. -->
        <!-- exported=false = HANYA bisa menerima intent dari internal app ini (tidak bisa di-trigger app lain = security). -->
        <!-- Pakai FQN wajib com.example.data.location.* (manifest namespace != actual source package). -->
        <receiver
            android:name="com.example.data.location.GeofenceEventReceiver"
            android:exported="false" />
        <activity
            android:name="com.example.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
